// 011 조건문 배우기 - if, else if, else

/**
    else if 와 else 는 if 의 결과값이 fasle 일 때 추가 실행되는 조건문이다.
    ==는 동등 연산자이다.
    == 를 가운데 두고 왼쪽과 오른쪽의 값을 비교한다. 동등 여부에 따라 결과값은 true 또는 false로 반환한다.
 */

 var number = 2;
 if (nummber == 1) {
     console.log('number는 1 입니다.');
 } else if (number == 2) {
     console.log('number는 2 입니다.')
 } else if (number == 3) {
     console.log('number 는 3 입니다.')
 } else {
     console.log('number는 1,2,3 중 해당되는 것이 없습니다.')
 }

 