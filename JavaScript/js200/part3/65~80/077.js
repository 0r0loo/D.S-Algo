// 077 특정 문자열 포함 여부 확인하기 (includes)

/**
 *  String 내장 객체의 메소드 includes는 일치하는 문자열이 있는 경우 true를, 없으면 false를 반환한다.
 * ㅁ   문자열.includes(문자열, 인덱스)
 *  두 번쨰 인자의 인덱스는 필수값은 아니다
 *  추가로 두 번쨰 인자에 인덱스를 지정하면, 해당 인덱스 위치에서부터 문자열을 확인한다,
 *  두번쨰 인자가 없으면 기본값 0 인덱스부터 문자열을 확인한다.
 */