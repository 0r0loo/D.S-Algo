// 070 문자열 자르기 3 (substr)

/*
    String 내장 객체의 substr 메소드는 인자로 시작 지점의 인덱스와 길이를 받는다.
    두 번쨰 인자인 종료 인덱스는 선택 사항이므로 필수값은 아니다.
    지정된 인덱스부터 시작해서 지정된 문자 수 또는 길이만큼의 새 문자열을 반환한다.
    따라서 추출하고자 하는 문자열의 길이를 정확히 알고 있는 경우 substr 메소드를 활용하는 것이 좋다.

    'string'.substr (startIdx, length)
*/